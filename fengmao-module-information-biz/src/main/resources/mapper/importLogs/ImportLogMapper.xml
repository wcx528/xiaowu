<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fmyd888.fengmao.module.information.dal.mysql.importLogs.ImportLogMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
     -->
    <insert id="batchInsertX" parameterType="list">
        INSERT INTO fm_import_log
        ( creator,  create_time,  updater,  update_time,  deleted,  dept_id,  task_id,  status,  err_message  )
        VALUES
        <foreach collection="list" item="item" separator=",">
            ( #{item.creator} ,  GETDATE() ,  #{item.updater} ,  GETDATE() ,   0  ,  #{item.deptId} ,  #{item.taskId} ,  #{item.status} ,  #{item.errMessage}   )
        </foreach>
    </insert>
</mapper>