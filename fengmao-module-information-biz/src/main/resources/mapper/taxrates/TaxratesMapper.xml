<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fmyd888.fengmao.module.information.dal.mysql.taxrates.TaxratesMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <select id="selectPageByKeyword" resultType="com.fmyd888.fengmao.module.information.controller.admin.taxrates.vo.TaxratesRespVO">
        SELECT
        t1.id,
        t1.tax_code,
        t1.tax_name,
        t1.tax_rate,
        t1.status,
        t1.remark,
        t1.create_time,
        t1.update_time
        FROM fm_taxrates t1
        LEFT JOIN fm_taxrates_dept t2 ON t1.id = t2.entity_id AND t2.deleted = 0
        <where>
            t1.deleted = 0
            <if test="pageReqVO.deptId != null and pageReqVO.deptId != ''">
                AND t2.dept_id = #{pageReqVO.deptId}
            </if>
            <if test="pageReqVO.taxName != null and pageReqVO.taxName != ''">
                AND t1.tax_name = #{pageReqVO.taxName}
            </if>
            <if test="pageReqVO.taxRate != null">
                AND t1.tax_rate = #{pageReqVO.taxRate}
            </if>
            <if test="pageReqVO.status != null">
                AND t1.status = #{pageReqVO.status}
            </if>
            <if test="pageReqVO.remark != null and pageReqVO.remark != ''">
                and t1.remark like concat('%',#{pageReqVO.remark},'%')
            </if>
        </where>
        GROUP BY t1.id, t1.tax_code, t1.tax_name, t1.tax_rate, t1.status, t1.remark, t1.create_time, t1.update_time
    </select>
</mapper>
